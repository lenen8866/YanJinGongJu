<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--从下而上Dialog出场动画主题样式-->
    <style name="MusicButtomAnimationStyle" parent="android:style/Theme.Dialog">
        <!-- 是否有标题-->
        <item name="android:windowNoTitle">true</item>
        <!--背景颜色及透明程度-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
        <!--是否模糊-->
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowCloseOnTouchOutside">true</item>
        <item name="android:windowAnimationStyle">@style/MusicButtomAnimation</item>
    </style>
    <!--从下而上动画-->
    <style name="MusicButtomAnimation">
        <!-- 入场动画 -->
        <item name="android:windowEnterAnimation">@anim/music_bottom_menu_enter</item>
        <!-- 出厂动画 -->
        <item name="android:windowExitAnimation">@anim/music_bottom_menu_exit</item>
    </style>
    <!--标题栏的返回按钮-->
</resources>